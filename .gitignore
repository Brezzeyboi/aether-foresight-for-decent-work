node_modules/

# TypeScript incremental build cache
*.tsbuildinfo

.vercel

# Editor and assistant tooling. Nothing here is needed to build or serve the
# site: the dev server runs from package.json, and these are regenerated by
# whatever tool wants them.
.claude/
.impeccable/
